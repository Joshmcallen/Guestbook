extends layout

block content
    h1.
        User List
    table
      thead
        username
        usermessage
        useremail

        each user, i in userlist
          tr
            td
                =user.username
            td
                a(href="mailto:#{user.email}")= user.username
            td
                =user.message
